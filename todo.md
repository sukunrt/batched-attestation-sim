- we want a few logs in the attprop system:
    - 1. log when enqueuing a message to a peer with all the info like push mesh or bitmap mesh,
         number of messages in flight etc
    - 2. log when sending a bitmap to the peer. this should be from the bitmap writer. 

